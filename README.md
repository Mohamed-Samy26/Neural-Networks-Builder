# Neural-Networks

## Installation

just run the following command in your terminal:

```powershell
pip install -r requirements.txt
```

## Getting started

Just run `main.py`

```powershell
python main.py
```
